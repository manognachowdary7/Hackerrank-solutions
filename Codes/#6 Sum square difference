def sumSquareDifferences(n):
    sumsquares = (n * (n + 1) * (2 * n + 1)) // 6
    squaresum = ((n * (n + 1)) // 2) ** 2
    diff = abs(sumsquares - squaresum)
    return diff
t = int(input().strip())
for a0 in range(t):
    n = int(input().strip())
    res = sumSquareDifferences(n)
    print(res)
